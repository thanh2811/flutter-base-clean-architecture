enum AppInputType { dropDown, datePicker, popUpShow, timePicker }

enum AppStringFormatType { sequence, title, allCaps, allLowerCase }

enum UriType { phone, website, email, zalo, sms }

enum DialogType { alert, datePicker }

enum ImageCropStyle { circle, rect, square }

enum ImageType { none, avatar, cover, square }

enum FilterType {
  status,
  typeOfAppointment,
  typeOfInteraction,
  priority,
  customer
}

enum ViewType { add, edit }
